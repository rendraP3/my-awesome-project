{
  "name": "awesome_project",
  "version": "1.0.0",
  "description": "awesome project",
  "main": "index.js",
  "directories": {
    "lib": "lib",
    "test": "test"
  },
  "scripts": {
    "setup": "npm install && npx lefthook install",
    "generate": "flutter pub run build_runner build --delete-conflicting-outputs",
    "init": "npm run setup && flutter pub get && npm run generate && cd configuration && flutter pub get && npm run generate"
  },
  "author": "Rendra",
  "license": "ISC",
  "devDependencies": {
    "@arkweid/lefthook": "^0.7.7"
  }
}
